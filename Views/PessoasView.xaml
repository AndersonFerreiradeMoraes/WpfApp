<UserControl x:Class="WpfApp.Views.PessoasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WpfApp.ViewModels"
             xmlns:conv="clr-namespace:WpfApp.Converters">

    <UserControl.Resources>
        <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:PessoasViewModel />
    </UserControl.DataContext>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filtros -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">

            <!-- TextBox Nome com Placeholder -->
            <Grid Width="200" Margin="0,0,8,0">
                <TextBox x:Name="txtNome" Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Nome"
                           Foreground="Gray"
                           Margin="4,0,0,0"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False"
                           Visibility="{Binding Text, ElementName=txtNome, Converter={StaticResource StringToVisibilityConverter}}" />
            </Grid>

            <!-- TextBox CPF com Placeholder -->
            <Grid Width="150" Margin="0,0,8,0">
                <TextBox x:Name="txtCpf" Text="{Binding FiltroCpf, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="CPF"
                           Foreground="Gray"
                           Margin="4,0,0,0"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False"
                           Visibility="{Binding Text, ElementName=txtCpf, Converter={StaticResource StringToVisibilityConverter}}" />
            </Grid>

            <Button Content="Salvar" Command="{Binding SalvarCommand}" Width="90"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Pessoas}" AutoGenerateColumns="False" CanUserAddRows="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True" Width="50"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="CPF" Binding="{Binding CPF}" Width="150"/>
                <DataGridTextColumn Header="Endereço" Binding="{Binding Endereco}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botão Excluir -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Excluir" Width="100" Click="Excluir_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
